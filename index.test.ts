import { BrexWebhooks } from "./index";

describe("brex-webhooks-typescript-sdk", () => {
    it("initialize client", async () => {
        const brexwebhooks = new BrexWebhooks({
        });
    });
});
